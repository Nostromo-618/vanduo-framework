# When we need to serve this project locally

 When we need to serve this project locally to try out some feature or test, lets use 'npx serve' , 'serve' is installed globally on this host.

## Guidelines

- Allways stop the 'npx serve' after we finish the test.

