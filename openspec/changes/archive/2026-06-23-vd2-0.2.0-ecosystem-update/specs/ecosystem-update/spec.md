# Ecosystem update (vd2 v0.2.0) — Spec

## Requirements

### R1 — OpenSpec change folder exists

`framework/openspec/changes/2026-06-23-vd2-0.2.0-ecosystem-update/`
exists on the local `vd2-0.2.0-release-prep` branch with:

- `proposal.md` describing the ecosystem reference update.
- `tasks.md` listing the work.
- `specs/ecosystem-update/spec.md` (this file).

### R2 — `framework/README.md` mentions vd2

`framework/README.md` contains a link or reference to the
`@vanduo-oss/vd2` package or its repository. The link should be
placed in or near the "Release Notes" section so downstream readers
can find the Vue 3 docs site.

### R3 — Branch and push discipline

- The work is committed on a branch named `vd2-0.2.0-release-prep`.
- `main` is untouched.
- No pushes were performed.
